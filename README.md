# 쿠팡 광고계산기

쿠팡 판매자센터의 **어제 판매 데이터**와 광고센터의 **어제 광고 데이터**를 모아
캠페인별·일자별 **광고 장부**(광고수익률, CPC, 광고 마진, 순이익 …)를 자동 계산합니다.

## 🟢 초보자는 여기만 보세요 — 크롬 확장 프로그램

프로그램 설치, 서버, 터미널 **필요 없음**. 크롬에 `extension` 폴더를 확장 프로그램으로 넣으면 끝입니다.

👉 **[초보자 가이드 (설치 5분, 매일 1분)](docs/초보자_가이드.md)**

매일 하는 일: 판매분석 → 어제 화면에서 **① 판매 데이터 저장**(상품별 판매 리포트를 받아 읽음), 광고 관리 → 매출 성장 → 어제 화면에서 **② 광고 데이터 저장**.
직접 받은 리포트 엑셀은 **📂 리포트 파일 올리기** 로 올리면 됩니다.
장부는 확장 아이콘 → **📒 장부 보기** 에서 봅니다. 마진이 바뀌면 적용 시작일과 함께 넣으면 그 날부터만 적용됩니다.

![대시보드](docs/img/v8_dash.png)

---

## 버전 기록 (확장 프로그램)
팝업과 장부 화면의 제목 옆에 현재 버전이 표시됩니다. 새 버전이 나오면 알림 배너가 뜨고, 배너의 **지금 업데이트** 버튼(또는 데이터·설정 → 🆕 업데이트)을 누르면 최신 파일을 받아 덮어쓰고 스스로 새로고침됩니다. 이 방식은 프로그램 폴더가 **다운로드 폴더 안**에 있을 때 됩니다(크롬 확장은 거기에만 파일을 쓸 수 있음). 다른 곳에 두었다면 **extension 폴더 안의 업데이트.bat** 을 더블클릭하세요.

| 버전 | 내용 |
|---|---|
| **v0.44.1** | 제로 ROAS 와 마진을 <b>함께 변경</b>: 효율 점검의 제로 ROAS 입력 칸에 '옵션 마진도 같이' (그 캠페인 옵션 전부, 같은 시작일), 캠페인·옵션 탭 '이 상품 전체 바꾸기'에 '제로 ROAS %' (같은 시작일로 캠페인 제로 ROAS 도 변경). |
| v0.44.0 | <b>예약 시각(기본 13:00) 전에는 '어제' 데이터를 받지 않음</b>: 쿠팡 숫자가 오전엔 덜 잡혀 있어서. 날짜를 정해 가져오기·기간 광고 보고서 받기·지금 자동 수집 실행·지금 광고 보고서 받기·보고서 파일 올리기 모두 예약 시각 전이면 그저께까지만(안내 표시). 어제 것은 예약 시각의 자동 수집에서 받음. |
| v0.43.3 | 버벅임 개선: 광고 보고서 행(가장 큰 데이터)을 따로 저장하고 메모리에 두어, 제로 ROAS·모드 같은 작은 저장에 전체 데이터를 다시 읽고 쓰지 않음. 제로 ROAS 입력에 <b>전체 기간에 적용 / 이 날부터</b> 선택(이력이 없으면 전체 기간이 기본). |
| v0.43.2 | 🎯 광고 효율 점검을 대시보드에서 <b>광고 장부 맨 위</b>로 옮김(접을 수 있음). 캠페인 분석 왼쪽 목록은 <b>운영 중인 캠페인만</b> 기본 표시. '중단·삭제된 캠페인도 보기'를 켜면 회색으로 함께 표시(개수 표시). |
| v0.43.1 | <b>엑셀 4번 시트 읽기 수정</b>: 맨 아래 이름 없는 빈 템플릿 블록이 바로 위 캠페인(49. 이불 건조 클립)에 붙어 0 으로 덮어쓰던 문제, 블록 중간 A열 메모('카피바라 였음')가 캠페인 이름으로 잡히던 문제 수정. 미리보기에 엑셀 '전체 순이익' 줄과의 대조 결과(불일치 일수) 표시 — 이 파일은 456일 전부 일치. |
| v0.43.0 | <b>지난 날짜 광고 값은 광고 보고서로 채움</b>: 광고 관리 화면은 어제만 열 수 있어 날짜를 정해 가져오기의 '광고'가 지난 날엔 실패했음 → 그 날 광고 보고서(키워드별 일별)가 있으면 캠페인별로 합쳐 광고비·클릭·전환매출·판매수를 장부에 넣음(노출수는 클릭 있는 키워드만이라 조금 적을 수 있음, 목표효율·예산은 없음). 보고서를 받을 때마다 자동으로 채워짐. |
| v0.42.3 | <b>캠페인 이름 합치기 규칙 축소(중요)</b>: v0.38.1 의 '다른 캠페인 이름을 품고 있으면 합침' 규칙이 '1. 타이머_236%_구'처럼 진짜 다른 캠페인까지 합칠 수 있었음 → 남는 글자가 배지·버튼 단어(AI 스마트광고, 오늘의 Pick, 베스트, 수정, 삭제 등)뿐일 때만 합침. 합친 내역은 저장(nameMerges). 이미 합쳐진 데이터는 엑셀 장부 다시 가져오기·광고 다시 수집으로 복구. |
| v0.42.2 | 광고 장부의 전체 순이익은 <b>광고 외 지출을 빼지 않음</b>(판매 마진 − 광고비, 엑셀과 같은 기준). 광고 외 지출은 대시보드 순이익·세후 순마진·광고 외 지출 탭에서만 반영. |
| v0.42.1 | 광고 장부 전체 줄을 세 줄로: <b>캠페인 순이익 합계</b>(판매 마진 − 광고비, 엑셀 마진계산기의 전체 순이익과 같은 기준) → 광고 외 지출(월 지출은 일수로 배분) → 전체 순이익. 엑셀과 비교할 때 첫 줄을 보면 됨. |
| v0.42.0 | 제로 ROAS 를 <b>캠페인마다 이력으로</b> 입력(✎ → 값 + 시작일 + 저장). 할인·가격 변경으로 바뀌면 시작일부터만 새 값이 적용되고 그 전 날짜는 예전 값으로 계산(광고 이익도 날짜별 제로로). 이력 목록·삭제. 안 넣은 캠페인은 기본값(250%). |
| v0.41.3 | 제로 ROAS: <b>기본값</b>(처음 250%, 효율 점검 카드 오른쪽 위에서 변경)을 쓰고 캠페인별로 직접 넣은 값이 있으면 그 값. 옵션 마진으로 계산한 값은 참고로만 표시하고 쓰는 값과 25% 넘게 다르면 알림. |
| v0.41.2 | <b>총 매출 이중 집계 수정</b>: 엑셀 확정 구간에서 옵션이 연결되지 않은 캠페인의 광고 매출이 총 매출에 한 번 더 더해지던 문제(판매 리포트가 있는 날은 리포트 매출만 씀). 대시보드 아래 <b>📊 월별 합계</b>(판매 리포트 원본 매출·판매량·반품취소·광고비·광고매출, 데이터 빠진 달 표시) — 쿠팡 판매분석 월별 숫자와 대조용. |
| v0.41.0 | 대시보드에 <b>🎯 광고 효율 점검</b>: 캠페인마다 <b>제로 ROAS</b>(손익분기 = 판매가×1.1÷마진, 옵션 마진으로 자동 계산 + 직접 입력)와 최근 3/7/14일 평균 ROAS 를 비교해 빨강(조정 필요)·초록(적정)·파랑(여유)으로 표시. 직전 같은 기간과 <b>노출·광고비·광고 이익 증감</b>, <b>목표 ROAS 를 바꾼 효과</b>(잘 됐는지/지나쳤는지) 판단. 캠페인별 <b>시즌/비시즌 모드</b>(시즌은 종료일이 가까울수록 기준을 빡빡하게, 비시즌은 이익 나는 것만). 효율 최고·조정 필요 캠페인 목록. 탭을 누르면 그 화면의 검색·필터·선택이 처음 상태로. |
| v0.40.1 | 모든 화면 맨 위에 <b>⚠️ 마진이 없는 광고 옵션 N개</b> 알림(운영 중 캠페인에 연결됐는데 마진이 없는 옵션, 캠페인별 개수, 최근 30일 판매된 것 개수). '마진 입력하러 가기'를 누르면 그 옵션만 걸러 보여 줌. 마진을 넣으면 바로 사라짐. |
| v0.40.0 | <b>광고 보고서·광고센터에 나온 옵션 자동 등록</b>: 운영 중인 캠페인이 광고한 옵션이 목록에 없으면 그 캠페인에 연결해 자동으로 넣음(마진만 비어 있어 '마진 없는 옵션' 할 일에 뜸). <b>기록하지 않을 판매</b>: 팔렸지만 목록에 없는 옵션에서 '무시' 버튼, 또는 무시 단어(재판매·리퍼 등)를 저장하면 그 판매는 장부·목록에서 빠짐(되돌리기 가능, 장부 위에 뺀 개수 안내). |
| v0.39.12 | 캠페인·옵션 탭에서 캠페인 하나를 골라 보면 표 위에 <b>그 캠페인이 광고하지만 목록에 없는 옵션</b> 카드가 뜸(광고 보고서·광고센터 기준, '쿠팡에서 옵션 가져오기'로 광고센터 상품 목록을 직접 읽어 옴). 옵션이 몇 개 빠졌는지 바로 확인·추가. |
| v0.39.11 | 캠페인 선택 상자의 <b>확인</b> 버튼을 눌러 선택을 적용(그동안은 바깥을 눌러 닫아서 선택이 취소되고 있었음). 기간 보고서 생성 대기 10분. |
| v0.39.10 | 새 버전 확인을 6시간 → 10분마다(앱을 열 때)로. 빠르게 고친 버전이 나와도 배너가 바로 뜨도록. |
| v0.39.9 | 전체선택: 체크박스 입력칸 클릭이 안 먹으면 '전체선택' 글자 바로 왼쪽에 그려진 상자와 그 줄도 눌러 봄. |
| v0.39.8 | 캠페인 전체 선택 다시 고침: '전체선택' 글자가 아니라 그 옆 <b>체크박스 칸 자체</b>를 누르고, 그래도 안 되면 항목 체크박스를 하나씩, 그래도 안 되면 항목 줄을 하나씩 누름. 새로 나타난 체크박스만 세어 실제 선택 여부를 판단(이전엔 다른 체크박스가 섞여 잘못 셌음). |
| v0.39.7 | 기간 보고서 실패 원인 수정: 기간을 바꾸면 캠페인 목록을 다시 불러오느라 비어 있는 사이에 '전체 선택'을 눌러 아무것도 선택되지 않았음 → 목록이 채워질 때까지 기다린 뒤 선택하고, 실제 체크된 개수를 확인(0개면 최대 3번 재시도). '보고서 만들기' 버튼이 비활성이면 그 이유를 기록에 남김. |
| v0.39.6 | 기간 보고서 진단 보강: 보고서 만들기를 누른 뒤 새로 뜬 글자(창·안내)를 기록에 남기고, 예약 창의 등록·확인 버튼까지 누름. |
| v0.39.5 | 광고 보고서 목록 줄 찾기를 실제 목록 모양('생성일 시작 ~ 끝 [일별] …')에 맞춤: 앞의 생성일은 무시하고 <b>기간 칸</b>이 시작~끝과 같은 줄을 찾음(v0.39.3 의 방식은 생성일 때문에 하루짜리도 못 찾을 수 있었음 → 수정). 기간이 길면 뜨는 <b>'보고서 예약' 확인 창</b>을 알아보고 눌러 예약 생성. |
| v0.39.4 | 지금 업데이트: 폴더를 자동으로 찾음(적은 폴더 → 윈도우 '압축 풀기'가 만드는 같은 이름 두 겹 폴더 → 다운로드 폴더 바로 아래). chrome://extensions 의 '소스' 경로를 통째로 붙여 넣어도 됨. |
| v0.39.3 | 기간 광고 보고서 받기 보강: 목록에서 줄을 찾을 때 글자 모양을 맞히는 대신 <b>줄에 적힌 날짜가 정확히 시작·끝인 줄</b>을 찾음(하루짜리를 찾을 때 30일짜리가 잡히지 않음). 기간 보고서는 생성 대기를 5분으로. 실패하면 화면 안내 문구(기간 제한 등)와 목록 위쪽 줄 내용을 기록에 남겨 원인을 볼 수 있음. |
| v0.39.2 | 캠페인·옵션 탭: 여러 줄에 마진을 입력하면 노란색으로 표시되고 아래에 <b>입력한 N개 모두 저장</b> 막대가 떠서 한 번에 저장. 저장하면 화면 아래 알림(무엇을 바꿨는지)이 뜨고 표 스크롤 위치가 유지돼 바뀐 줄이 바로 보임. |
| v0.39.1 | 캠페인·옵션 탭 정리: 긴 설명은 '도움말'로 접고, 잘 안 쓰는 버튼은 '더 보기' 메뉴로, 마진 일괄 저장 칸은 필요할 때만, 상품 묶음의 캠페인·마진 도구는 '이 상품 전체 바꾸기' 를 눌렀을 때만. 위에 <b>할 일</b> 알약(새 캠페인·마진 없음·팔렸지만 목록에 없는 옵션·제안)으로 바로 이동. '목록에 없는 판매 옵션' 을 '🛒 팔렸지만 목록에 없는 옵션' 으로 이름 바꾸고 설명 추가. 두 보조 목록은 표 아래로. |
| v0.39.0 | <b>앱에서 버튼 하나로 업데이트</b>: 데이터·설정 → 🆕 업데이트 → '지금 업데이트 (한 번에)'. 프로그램 폴더가 다운로드 폴더 안에 있으면(ZIP 을 그 자리에서 푼 경우) 최신 파일을 받아 덮어쓰고 스스로 새로고침(업데이트.bat 불필요). 새 버전 알림 배너에도 '지금 업데이트' 버튼. 광고 장부 아래 <b>🛒 광고 없이 팔린 옵션</b> 목록(윙 판매·중단/삭제 캠페인 판매, 마진 없으면 표시, '마진 입력' 버튼으로 바로 이동). 옮긴 옵션의 예전 캠페인이 광고비를 쓴 날의 판매는 예전 캠페인 줄에 남김. |
| v0.38.1 | 캠페인 이름 정리 보강: 규칙으로 못 떼는 배지가 붙어도 번호로 시작하는 다른 캠페인 이름을 품고 있으면 그 이름으로 합침('오늘의 Pick31. 피크닉매트' → '31. 피크닉매트'). 광고 입력 화면에 <b>캠페인 이름 정리</b> 버튼(무엇을 합쳤는지 표시). |
| v0.38.0 | 캠페인을 지우고 새로 만들면 옵션이 <b>최근 캠페인으로 자동으로 옮겨짐</b>(광고 보고서·광고센터에서 새 캠페인이 그 옵션을 광고한 것이 확인될 때, 마진은 그대로). 광고 장부는 연결 캠페인이 그날 광고비를 안 썼거나(중단) 사라진(삭제) 날의 판매를 <b>'(광고 없는 판매)'</b> 줄로 모아 광고 없이 팔린 마진으로 보여 주고, 기간 안에 광고비를 쓰지 않은 캠페인 줄은 자동으로 숨김. 캠페인·옵션 탭도 운영 중인 캠페인만 기본 표시(중단·삭제된 캠페인은 선택 상자에서 따로). |
| v0.37.2 | 광고센터 캠페인 이름에 배지('AI 스마트광고', '오늘의 Pick')와 마우스를 올리면 나오는 '수정 삭제' 글자가 붙어 다른 캠페인으로 갈라지던 문제 수정. 이름 링크 글자만 읽고, 이미 저장된 이름도 앱을 열면 자동으로 정리해 같은 캠페인으로 합침. |
| v0.37.1 | 캠페인 분석 차트에서 ROAS 점선이 차트 밖으로 튀어나가던 문제 수정(오른쪽 눈금을 최댓값 위까지 잡고 그림 영역으로 잘라냄). 이 기간 광고 보고서 받기가 최근 구간부터 거꾸로 받고 진행 기록·중단이 되며, 빠른 선택에 최근 90일·최근 1년 추가 — 쿠팡이 주는 데까지 전부 받다가 없으면 스스로 멈춤. |
| v0.37.0 | 데이터·설정 → 날짜를 정해 가져오기에 <b>이 기간 광고 보고서 받기</b>: 시작~끝 기간의 키워드·옵션별 일별 보고서를 광고센터에서 만들어 받아 캠페인 분석에 넣음(31일씩 나눠 요청, 이미 만들어진 같은 기간 보고서가 있으면 바로 다운로드). 프로그램을 쓰기 전 기간도 가능. |
| v0.36.0 | 캠페인·옵션 탭: <b>캠페인 선택 상자</b>로 그 캠페인 옵션만 보기(캠페인 없음 포함). <b>새 마진 일괄 저장</b>: 검색·캠페인·필터로 걸러진 옵션 전부에 새 마진·적용 시작일·사유를 한 번에. 상품 묶음 머리줄에도 '이 상품 전체에 새 마진 적용'(시작일 포함). |
| v0.35.0 | 캠페인 분석 기본통계를 시크릿팡식으로: <b>기간 비교</b> 카드(광고비·광고매출·ROAS·클릭률·전환율·CPC, 이전 기간 대비, 추세선, 전체/검색/비검색 필터), <b>노출 영역별</b>(검색·비검색·합계) 표에 노출·클릭·주문·클릭률·전환율·CPM·CPC·광고비(%)·광고매출(%)·ROAS·전환당비용·객단가·마진, 통합·매출·광고비·노출수·클릭률·전환율·CPC 차트를 검색/비검색으로 나누고 총ROAS 선을 겹침. |
| v0.34.3 | 광고 장부의 트래픽 슬롯 칸을 접을 수 있게(기본 접힘, 펼친 상태 기억). |
| v0.34.2 | 광고 보고서 자동 받기 성공 확인(캠페인 분석에 키워드 채워짐). 키워드 '-'(검색어 없이 노출된 비검색 지면 광고)를 '비검색 노출 (키워드 없음)' 으로 표시하고 설명 툴팁, 제외 키워드 후보에서 제외. |
| v0.34.1 | 광고 외 지출 사유에 '3PL' 추가. |
| v0.34.0 | 광고 보고서: 화면 흐름은 전부 통과했으나(기간 설정→날짜→일별→캠페인 전체→만들기→9초 뒤 목록→다운로드) 파일이 화면에 붙지 않은 링크로 내려와 가로채지 못함 → 파일(blob)이 만들어지는 순간 바로 잡아 저장(같은 파일 중복 제거, 엑셀/CSV 만). 보고서 단위를 '캠페인 › 광고그룹 › 상품 › 키워드' 라디오로 선택, 라벨 밖 체크박스도 찾음. |
| v0.33.1 | '지금 광고 보고서 받기' 버튼을 '지금 자동 수집 실행' 옆으로. |
| v0.33.0 | 광고 보고서 자동 받기 완성 단계: 보고서 화면(marketing-reporting/billboard/reports/pa)을 찾았고, 쿠팡이 blob 방식으로 내려주는 파일을 페이지 안에서 가로채 읽음(다운로드는 그대로). 흐름: '요청한 보고서' 목록에 어제 날짜 키워드 보고서가 있으면 그 줄의 다운로드 → 없으면 기간 설정·시작일/종료일=어제·일별·키워드 포함·캠페인 전체 선택·보고서 만들기 → 목록 새로 고침하며 최대 2분 기다려 다운로드. 단계별 결과 기록. |
| v0.32.3 | 광고 보고서 화면 찾기: 메뉴가 링크가 아닌 버튼이라(기록: "보고서 관련 링크: 없음") 상위 메뉴에 마우스를 올린 뒤 '광고보고서' 를 실제 클릭 방식으로 누르고, 그래도 안 되면 광고센터 스크립트 파일에서 'report' 가 든 화면 경로를 찾아 차례로 열어 봄. |
| v0.32.2 | '옵션이 연결되지 않은 캠페인' 은 최근 60일 광고센터 목록·보고서에 있는 캠페인만(엑셀 시절 끝난 캠페인 제외). 옵션 가져오기: 캠페인 이름이 든 줄의 실제 링크 주소로 이동하고, 없으면 이름을 눌러 봄. 실패 시 그 줄의 링크·구조를 기록. |
| v0.32.1 | 캠페인·옵션 탭의 '옵션이 연결되지 않은 캠페인' 패널을 기본 닫힘으로. |
| v0.32.0 | 캠페인·옵션 탭 '옵션이 연결되지 않은 캠페인' 에 <b>쿠팡에서 옵션 가져오기</b>: 광고센터를 열어 캠페인을 누르고 상품 목록의 옵션명·ID("ID: 95988650186")를 읽어 후보로 채움(한 캠페인씩 또는 모두). 마진만 넣고 추가하면 끝. |
| v0.31.1 | 광고 보고서 받기: 대시보드 주소가 '보고서 주소'로 잘못 저장돼 메뉴 단계를 건너뛰던 문제 → 지금 화면이 보고서 화면인지(주소·버튼으로) 확인하고, 아니면 설정 주소를 지우고 메뉴 링크(숨은 메뉴 포함) → 후보 주소(/marketing/report 등) 순으로 찾음. 확인된 주소만 저장. 실패 시 화면의 '보고서' 관련 링크 주소를 모두 기록. |
| v0.31.0 | 캠페인·옵션 탭: 캠페인 번호 오름차순 정렬(등록순 선택 가능). <b>옵션이 연결되지 않은 캠페인</b> 패널: 새로 만든 캠페인이 뜨고, 광고 보고서에 있으면 그 캠페인이 광고한 옵션ID·옵션명이 후보로 나와 마진만 넣고 추가(모두 한 번에도). 보고서가 없으면 옵션ID 직접 입력. |
| v0.30.1 | 광고 보고서 받기: 메뉴 '광고보고서' 가 흉내 클릭에 반응하지 않아(주소 그대로) 대시보드에서 키워드·다운로드를 찾던 문제 → 메뉴 링크의 실제 주소를 읽어 직접 이동하고 알아낸 주소를 설정에 저장. 보고서 종류는 선택 상자에서 '키워드' 를 고르고(없으면 글자 클릭). 실패 시 보고서 화면의 버튼 목록을 기록. |
| v0.30.0 | <b>계정 확인</b>: 설정에 '광고 계정 이름'(예: DUGN)·'판매자센터 상호'를 적어 두면 다른 계정 화면이 열렸을 때 저장하지 않고 이유를 남김(계정 전환 메뉴가 있으면 이름을 눌러 전환 시도). 시험을 누르면 화면의 이름 후보를 알려 줌. 광고 입력에 '이 날짜 광고 전체 삭제'. <b>광고 보고서 자동 받기</b>(13시 수집 3단계): 보고서 메뉴 → 어제 → 키워드 → 다운로드를 글자로 찾아 누르고 새 창 가로채기로 받아 캠페인 분석에 저장. 실패하면 그 화면의 버튼 목록을 기록에 남김. 설정에 보고서 주소·켜기·지금 받기. |
| v0.29.0 | 🔎 <b>캠페인 분석</b> 탭(1단계): 광고센터 보고서 파일(키워드·옵션별 일별)을 올리면 캠페인별로 기본통계(일별 노출·클릭·클릭률·주문·전환율·CPC·광고비·광고매출·ROAS), 팔린 옵션, 전체 옵션, 전체 키워드(정렬·검색)를 봄. 키워드를 체크해 <b>제외 키워드 담기</b>, '광고비만 나가는 키워드 고르기' 추천, 담은 목록 복사(줄바꿈)·등록 완료 표시. 광고센터 자동 등록은 다음 단계. 데이터·설정에 '📢 광고 보고서 파일 올리기', 다운로드 자동 인식. |
| v0.28.0 | 광고 목록 일부만 읽히는 문제(기록: "페이지당 20 개 (적용 안 됨), 2쪽으로 넘어가도 내용이 바뀌지 않음, 1쪽/2쪽 10건"): 페이지당 개수 변경을 최대 3번, 쪽 넘기기를 2번 다시 시도하고, 그래도 끝 쪽까지 못 읽었거나 화면의 전체 개수보다 적으면 창을 새로 열어 다시(최대 3번). 마지막에도 일부면 저장하되 기록에 ⚠ 표시. 광고 입력 탭의 빈 줄이 무엇인지 안내. |
| v0.27.2 | 대시보드 지표 카드의 설명(광고 외 지출, 세금, 이전 대비 등)을 한 줄에 섞지 않고 줄마다 따로 표시. |
| v0.27.1 | 자동 로그인 켜짐/꺼짐을 색 동그라미와 초록/빨강 글씨로 표시(제목 옆과 자동 수집 상태 줄). |
| v0.27.0 | 광고센터 로그인이 풀리면 뜨는 <b>계정 선택 화면</b>(advertising.coupang.com/user/login, '로그인하기' 세 개)을 인식해 왼쪽 '쿠팡 윙 판매자' 버튼을 대신 누르고, 이어지는 윙 로그인까지 처리한 뒤 원래 화면으로 복귀. 광고 장부의 '전체 순이익' 줄을 날짜 머리글 아래에 고정(아래로 내려도 항상 보임). |
| v0.26.2 | 재시도 기록에 화면 상태를 끝까지 남김(80자에서 잘리던 것). 판매 리포트가 두 번 받아지던 것 마저 정리. |
| v0.26.1 | 자동 로그인 실제 동작 확인(판매 09/05 저장). 광고 탭에 스크립트를 못 넣던 경우: 크롬 오류 페이지면 새로 고쳐 다시 시도, 쿠팡이 아닌 주소면 그 주소를 기록에 남김. 실패 문구에 탭 주소·제목·상태 포함. |
| v0.26.0 | 🔐 <b>자동 로그인(선택)</b>: 13:00 수집 때 로그인 화면이면 저장해 둔 아이디·비밀번호로 대신 로그인하고 원래 화면으로 돌아가 수집. 4시간마다의 로그인 확인 때도 세션이 풀려 있으면 미리 로그인. 정보는 이 PC 의 크롬 안에만 저장(동기화 안 함), 인증번호·로봇 확인이 뜨는 날은 직접 로그인 필요. 로그인 화면 판정을 비밀번호 칸 유무로 단순화. |
| v0.25.0 | 13:00 예약이 그냥 지나가는 일 방지: 알람 하나에만 맡기지 않고 <b>매시간</b> '어제 데이터를 받았나' 확인해 안 됐으면 바로 실행(PC 잠듦·업데이트 재시작으로 알람을 놓친 경우). 예약·실행 시작을 기록에 남기고, 설정 화면에 '오늘 완료 / 아직 안 됨' 표시. 4시간마다 로그인 상태를 확인해 풀려 있으면 미리 알림. |
| v0.24.1 | 쿠팡 화면이 덜 그려진 채 열려 실패하면(글자 1568자짜리 광고센터) 사람이 다시 누르지 않아도 되게: 창을 새로 열어 바로 한 번 더 시도하고, 그래도 실패한 종류는 10분 뒤 자동으로 다시(하루 최대 3번). 광고는 최대 90초까지 기다림. 로그인 화면이면 기록에 그렇게 표시. |
| v0.24.0 | 광고 17건 중 절반이 성과 0으로 저장되던 문제: 페이지당 개수를 바꿔 새로 나타난 줄은 성과 숫자가 뒤늦게 채워지는데 바로 읽었음 → 새 줄이 나타난 뒤에도 모든 칸이 1.5초 간격 두 번 같고 성과 0인 줄이 없을 때까지(최대 10초) 기다림. 그래도 0인 캠페인은 3초 뒤 한 번 더 읽어 숫자가 있는 쪽으로 채우고, 몇 개를 채웠는지 기록에 남김. |
| v0.23.2 | 자동 수집 판매·광고 모두 확인(119건 / 17건). 리포트가 두 번 받아지던 것 정리, 기간 수집 요약 문구를 '빠진 날 없음' 등으로 알기 쉽게. |
| v0.23.1 | 광고 목록 2쪽째를 못 읽어 10건만 저장되던 문제(기록: "2쪽/2쪽 10건"): 쪽 이동·페이지당 개수 변경 뒤 표 내용이 실제로 바뀔 때까지(최대 6초) 확인하고, 안 바뀌면 다음 버튼 등 다른 방법으로 다시 시도. 화면에 적힌 전체 개수와 읽은 개수가 다르면 기록에 남김. |
| v0.23.0 | 광고 장부에 목표·예산만 있고 광고비·노출·클릭이 0으로 저장되던 문제: 광고센터가 목록을 먼저 그리고 성과 숫자를 뒤에 채우는데 그 전에 읽었음 → 성과 숫자가 들어오고 3초 간격 두 번 읽은 값이 같아질 때까지 기다림. 모두 0이면 저장하지 않음. 이미 있는 숫자는 0으로 덮어쓰지 않음(설정값만 갱신). 여러 쪽을 끝까지 못 읽었으면 한 번 더 읽음. |
| v0.22.2 | 자동 수집 판매 저장 확인("window.open 로 받은 … 판매 119건 저장"). 같은 리포트 주소가 잇달아 두 번 잡히면 한 번만 받음. |
| v0.22.1 | 가로챈 리포트 주소가 `/fcc/download-manager/…` 같은 상대 주소여서 "Invalid URL" 로 실패하던 것 수정(절대 주소로 변환). 광고 저장 기록에 읽은 쪽수 표시. |
| v0.22.0 | 자동 수집(판매): 확장이 연 창에는 사용자 클릭이 없어 리포트를 새 창으로 여는 다운로드가 팝업 차단에 걸림 → 페이지에 훅을 심어 새 창 주소를 가로채 확장이 직접 받아 저장. 자동 수집(광고): 화면 기간이 어제 하루가 아니면(다른 날·여러 날 합계) '어제' 를 다시 누르고, 그래도 아니면 저장하지 않음(엉뚱한 날짜로 저장되던 문제). |
| v0.21.0 | 자동 수집이 배경 탭에서 실패하는 원인 대응: 크롬은 보이지 않는 탭의 화면을 그리지 않아 광고 표가 비고 다운로드가 시작되지 않음 → 기본으로 <b>작은 창을 따로 띄워</b> 수집하고 끝나면 닫음(설정에서 끌 수 있음). 판매 다운로드는 30초 안에 안 오면 한 번 더 누름. 실패 기록에 그때 화면 상태(제목·글자 수·캠페인/다운로드 글자 유무)를 남김. |
| v0.20.0 | 자동 수집 실패 원인 수정: 광고 관리 주소가 도메인만(https://advertising.coupang.com/)으로 저장돼 있으면 기본 목록 주소로 자동 복구. 백그라운드 탭에서 화면이 늦게 떠도 최대 45초 더 기다리며 다시 읽고, 광고는 모든 페이지를 읽음. 판매는 다운로드 버튼을 3번까지 다시 찾음. 설정 주소 옆에 <b>기본 주소로</b>·<b>시험</b> 버튼(그 주소를 열어 표/다운로드 버튼을 찾는지 확인). '어제' 버튼을 기간 선택기를 열어서도 찾음. |
| v0.19.1 | 자동 수집 칸 안으로 <b>날짜를 정해 가져오기(시작~끝)</b> 를 옮김. 대기(초)·빠진 날 보충의 뜻을 화면에 설명. |
| v0.19.0 | 장부의 반품·취소 수를 빠져나간 수답게 -1 처럼 표시. 자동 수집에 <b>특정 날짜 수집</b>, 지난 날짜 채우기에 빠른 선택(어제·3·7·14·30일, 마지막 저장 다음 날~어제). 고른 날짜와 화면의 날짜가 다르면 저장하지 않고 알림. |
| v0.18.0 | 판매 리포트의 '총 판매수'·'총 취소된 상품수' 열을 그대로 읽음(취소는 음수로 오므로 절댓값). 자연 판매 수 = 총 판매 수 − 광고 전환 판매 수 로 같은 기준끼리 비교. 장부에 '총 판매 수' 항목 추가(기본 꺼짐), 판매 수 줄 이름에 뜻 설명 툴팁. |
| v0.17.0 | 대시보드에 **🔎 데이터 점검**(최근 14일 저장 상태와 '왜 안 보이는지' 안내). 저장 데이터가 보는 기간보다 뒤 날짜면 자동으로 그 날까지 보기, 광고비 0원 저장·마진 없는 판매 경고. 자동 수집 상태(켜짐/다음 예정/마지막 실행) 표시, 크롬이 꺼져 놓친 날은 켠 뒤 따라잡기. |
| v0.16.1 | 트래픽 효과 탭을 데이터·설정 위로 옮기고 '트래픽 켠 뒤 자연 매출이 늘었는지' 한 문장으로 요약. |
| v0.16.0 | 트래픽 효과 탭 신설, 지출 사유에 택배·인증 추가, 광고 장부 기본 표시에 노출수·클릭률·전환율. |
| v0.15.0 | 캠페인별 트래픽 슬롯 입력과 장부·대시보드 표시. |
| v0.14.4 | 광고 장부 위쪽에도 가로 스크롤바, 열 때 가장 최근 날짜로 이동. |
| v0.14.3 | 장부 맨 위 줄을 '전체 순이익' 하나로 (광고비·광고 외 지출 모두 차감). |
| v0.14.1 | 판매 수 줄을 광고 전환 판매 수 → 자연 판매 수 → 반품·취소 수 → 실제 판매 수 순으로. 반품·취소 수 = 리포트에 열이 있으면 그 합, 없으면 광고 전환 판매 수가 실제 판매 수를 넘는 만큼. |
| v0.14.0 | 판매 리포트에 반품·취소 열이 있으면 읽음. 가져온 파일의 열 이름을 기록에 남김. 광고 장부 표를 내용 폭만큼만 써 캠페인 칸이 늘어나지 않게. |
| v0.13.4 | 광고센터 표의 '종료일' 열을 날짜 열로 오인해 캠페인이 종료일 날짜로 저장되던 문제 수정(날짜 열은 날짜/일자/기준일만, 종료·시작·등록 등 제외). 업데이트 시 잘못 저장된 광고 행 1회 정리. |
| v0.13.3 | 광고센터 캠페인 목록(react-table) 전용 인식기, 인식기별 예외 격리·오류 표시. |
| v0.13.2 | ①: 판매분석 필터 줄을 표로 오인하던 문제 수정(옵션ID 헤더 필요), 행이 없으면 리포트 다운로드로 자동 전환. ②: 실패 시 광고보고서 다운로드 안내, 진단에 프레임·shadow DOM·반복 줄 구조 추가. 기본 광고 관리 주소를 /marketing/dashboard/sales 로. |
| v0.13.1 | 팝업: 저장 중 오류를 그대로 표시, '진단 정보 복사' 버튼(버전·탭·표 인식·데이터 상태·기록), 엑셀 확정 구간 날짜 저장 시 안내. |
| v0.13.0 | '광고 외 지출' 탭: 날짜·사유(트래픽/마케팅/체험단/촬영·디자인/포장·부자재/기타/직접 입력)·금액·메모, 월에 나눠 반영 또는 그 날에 반영. 대시보드 순이익·세후 순이익, 세후 순마진 탭, 광고 장부 합계 줄(광고 외 지출, 지출 차감 순이익)에 자동 반영. |
| v0.12.3 | 엑셀 가져오기가 3번 시트(옵션별 실제 판매)도 빠짐없이 저장 — 장부 숫자는 4번 시트, 매출·자연 매출은 3번 시트. 미리보기·되돌리기에 판매 행 포함. |
| v0.12.2 | 캠페인 순서를 이름 앞 번호순으로 고정. 엑셀 확정 구간에서도 총 매출·자연 매출은 판매 리포트 데이터에서 가져오도록 분리(장부 숫자는 4번 시트 유지). |
| v0.12.1 | 엑셀 4번 시트가 있는 마지막 날짜까지는 엑셀 확정값만 사용(그 구간의 ①② 데이터 무시). 광고 장부에 캠페인 선택(전체/한 캠페인, ◀▶ 이동). |
| v0.12.0 | 캠페인·옵션 목록을 엑셀 1번 시트/직접 추가한 옵션만 유지 (판매 리포트 옵션 자동 등록 제거, 기존 자동 등록분은 업데이트 시 1회 정리). '목록에 없는 판매 옵션' 에서 골라 추가, '마진 없는 옵션 정리' 버튼. |
| v0.11.1 | 캠페인·옵션: 상품별로 묶어 보기, 같은 상품의 다른 옵션이 연결된 캠페인을 제안하고 '제안 모두 적용' / 상품 단위 일괄 적용. 캠페인 없는 옵션의 판매를 '(캠페인 없음)' 줄로 집계해 총 매출·자연 판매에 포함. |
| v0.11.0 | 광고 장부 캠페인 표시 설정: 캠페인마다 자동 / 항상 표시 / 숨김 (시즌 종료 캠페인 정리), 장부 줄에서 바로 숨기기, 최근 30일 광고비 0인 캠페인 일괄 숨김, 대시보드 표에도 반영(합계는 유지). |
| v0.10.3 | 기간 선택에 최근 90일 · 180일 · 1년 추가. |
| v0.10.2 | 대시보드 맨 왼쪽에 '세후 순이익' 카드 — 선택 기간의 순이익에 그 해 누적 기준 증분 세금을 적용. |
| v0.10.1 | 세후 순마진: 가족 구성(배우자, 자녀 출생연도)으로 기본공제·자녀세액공제 자동 계산 (기본값 배우자 + 2022년생 1명 → 기본공제 450만, 자녀세액공제는 8세부터). |
| v0.10.0 | '세후 순마진' 탭(맨 왼쪽): 연도별 종합소득세 추정 — 근로소득(총급여)과 쿠팡 순이익을 합산해 세율표·근로소득공제·근로소득세액공제·청년창업 세액감면(기본 50%)·최저한세·지방소득세를 반영, 쿠팡 몫 세금은 증분 방식. 월별 세후 순이익, 계산 내역, 가정 편집(연도별 저장). |
| v0.9.1 | 업데이트 스크립트를 extension 폴더 안으로 이동(`extension/업데이트.bat`), 배치 파일을 영문만으로 작성해 코드페이지 문제 제거, 실패 원인과 로그(update.log) 표시, 실행 중인 배치 파일은 덮어쓰지 않음. |
| v0.9.0 | 엑셀 장부 가져오기를 4번 시트(확정 장부) 기준으로 변경 — 수정해 둔 마진이 반영된 값을 그대로 사용, 1번 시트 마진은 엑셀 마지막 날 다음 날부터 적용. 미리보기 → 적용, 가져오기 기록에서 되돌리기 / 장부 값 삭제, 모든 데이터 지우기. `tools/excel_to_backup.py` 도 4번 시트 기준(`--raw` 로 예전 방식). |
| v0.8.1 | 업데이트 간소화: `업데이트.bat`(맥 `업데이트.command`) 더블클릭으로 최신 파일 교체, 확장 프로그램이 새 버전을 6시간마다 확인해 알리고 파일이 바뀌면 스스로 새로고침. |
| v0.8.0 | 화면 전면 재설계(대시보드: KPI 카드·일별 차트·캠페인별 성과표·캠페인 상세), 자연 매출/자연 판매 지표, 예전 엑셀 장부 가져오기, 기간 선택, 장부 표시 항목 선택. `tools/excel_to_backup.py` 로 엑셀 → 백업 JSON 변환. |
| v0.7.0 | 광고센터 캠페인 목록 정식 대응: 칸 단위 격자 인식(머리글의 ? 아이콘·부제 무시), '305.19% 목표 350%' 칸에서 목표효율 추출, 집행 광고비/오늘 누적광고비 구분, 페이지당 개수를 최대로 바꾸고 여러 페이지를 넘겨 합침, 보고서 파일의 행별 날짜 지원. |
| v0.6.0 | 광고센터 대응: 표가 아닌 반복 줄(div 격자)·shadow DOM 도 읽는 인식기, ② 실패 시 다운로드 버튼 시도(파일 내용으로 판매/광고 자동 구분), '붙여넣기로 저장'(드래그 복사한 표), 페이지 진단 정보 표시. |
| v0.5.1 | '상품별 판매 리포트' 메뉴가 마우스 올림으로 열리는 경우 대응(hover→click→부모 순서로 시도), 실패 시 화면에 보인 항목을 진단으로 표시, 직접 받은 다운로드도 2분 안이면 자동 저장. |
| v0.5.0 | 특정 날짜·기간 수집: 팝업의 데이터 날짜 + '이 날짜 화면 열기', '지난 날짜 한꺼번에 채우기'(빠진 날 확인·자동 수집), 파일별 날짜 확인 후 가져오기, 자동 수집 시 최근 N일 빠진 날 보충. |
| v0.4.0 | 확장 새로고침 뒤 쿠팡 탭을 새로고침하지 않아도 되게 읽기 스크립트를 자동 주입. 판매분석 주소의 `start_date`/`end_date` 로 날짜 인식. 자동 수집 주소에 `{date}` 자리표시자. 버전 표시. |
| v0.3.0 | 판매분석은 '엑셀 다운로드 → 상품별 판매 리포트' 파일로 저장 (① 버튼이 대신 클릭, 다운로드 자동 감지). 리포트 파일 올리기. 라이브러리 없는 xlsx 리더. |
| v0.2.0 | 서버 없이 확장 프로그램 단독 동작 (저장·계산·화면 내장), 마진 이력, 백업/복원, 자동 수집. |
| v0.1.0 | 첫 버전 (파이썬 서버로 전송). |

## 🔧 고급: 파이썬 프로그램 (선택)

아래는 같은 계산을 PC 에서 파이썬으로 돌리는 버전입니다. 확장 프로그램만 써도 충분하며,
데이터를 파일(SQLite)로 갖고 싶거나 Playwright 로 브라우저를 자동 조작하고 싶을 때만 필요합니다.

## 왜 이렇게 만들었나
쿠팡 Open API(윙 API 키)는 상품·주문·정산 위주라 **광고 성과와 판매분석(방문자·조회·장바구니)은 제공하지 않습니다.**
그래서 데이터는 (1) 다운로드 파일 업로드, (2) 크롬 확장 프로그램이 로그인된 화면의 표를 읽어 전송, (3) Playwright 자동화
세 경로 중 하나로 들어오고, 계산·저장·조회는 이 프로그램이 맡습니다.

## 무엇을 계산하나

캠페인 × 날짜마다 (원래 엑셀 4번 시트와 같은 항목):

| 항목 | 계산 |
|---|---|
| 목표효율, 광고예산, 노출수, 클릭률, 전환율, 광고전환 판매수 | 광고센터 값 그대로 |
| 광고수익률 | 광고전환 매출 ÷ 집행 광고비 |
| 집행 광고비*10% | 집행 광고비 × 1.1 (부가세) |
| CPC 단가 | 집행 광고비 ÷ 클릭수 |
| 실제 판매 수 | 캠페인에 연결된 옵션들의 판매량 합 |
| 광고 마진 | Σ (옵션 판매량 × **그 날짜에 적용되는 옵션 마진**) |
| 순이익 (광고비제외) | 광고 마진 − 집행 광고비*10% |
| 월 합계 | 광고비·판매수·마진·순이익 합계, 광고수익률은 월 누계로 재계산 |

### 마진 이력 (가격 인하 · 쿠폰 대응)
옵션 마진은 **적용 시작일**과 함께 저장됩니다. 마진이 바뀌면 새 마진과 시작일만 추가하세요.
시작일 이전 날짜는 예전 마진으로, 시작일부터는 새 마진으로 계산되므로 과거 장부가 흔들리지 않습니다.
(웹 화면 → 옵션 · 마진 관리 → 마진 변경)

## 설치

```bash
python -m venv .venv
.venv\Scripts\activate          # Windows  (macOS/Linux: source .venv/bin/activate)
pip install -r requirements.txt
python -m playwright install chromium   # 자동 수집을 쓸 때만
```

`config/mapping.csv` 에 옵션ID · 상품명 · 광고 캠페인 · 옵션마진을 채워 두면 첫 실행 때 DB 로 들어갑니다.
(그 뒤로는 웹 화면에서 관리하고, `python -m coupang_calc mapping-export` 로 CSV 백업을 받을 수 있습니다.)

## 사용

### 웹 화면
```bash
python -m coupang_calc serve        # http://127.0.0.1:8765 가 자동으로 열립니다
```
- **광고 장부**: 기간 선택, 캠페인별 × 일자별 표, 월 합계, 전체 순이익, CSV 내보내기
- **일별 광고 입력**: 광고센터 → 광고 관리 → 매출 성장 → 어제 화면의 숫자를 캠페인별로 입력
- **옵션 · 마진 관리**: 옵션 ↔ 캠페인 연결, 마진 이력(적용 시작일) 관리
- **데이터 가져오기**: 판매분석 → 어제 → 엑셀 다운로드 → *상품별 판매 리포트* 파일 업로드, 광고 CSV 업로드, 자동 수집 실행

### 1단계: 파일로 가져오기 (반자동, 로그인 문제 없음)
```bash
python -m coupang_calc import --sales "상품별 판매 리포트.xlsx" --date 2025-06-08
python -m coupang_calc ads-template --date 2025-06-08     # data/inbox/ads_2025-06-08.csv 생성 → 숫자 채우기
python -m coupang_calc import --ads data/inbox/ads_2025-06-08.csv
python -m coupang_calc report --date 2025-06-08           # 터미널에서 확인
```
`--date` 를 생략하면 파일명에 있는 날짜(예: `sales_2025-06-08.xlsx`)를 쓰고, 없으면 **어제**로 봅니다.
같은 날짜를 다시 넣으면 덮어씁니다(취소 반영 등으로 숫자가 바뀌어도 중복되지 않음).

### 2단계: 크롬 확장 프로그램에서 서버로도 보내기
확장 프로그램은 기본적으로 크롬 안에만 저장합니다. 파이썬 서버에도 같은 데이터를 쌓고 싶으면
장부 보기 → 백업 · 설정 → "고급: 파이썬 서버로도 보내기" 를 켜고 `python -m coupang_calc serve` 를 실행해 두세요.
서버가 꺼져 있으면 확장 프로그램이 큐에 보관했다가 다음에 다시 보냅니다.

### 3단계(대안): Playwright 자동 수집 (별도 브라우저 자동화)
```bash
python -m coupang_calc login      # 최초 1회: 창이 뜨면 판매자센터·광고센터에 로그인 → 세션이 data/browser_profile 에 저장
python -m coupang_calc run        # 전일 데이터 수집 (창 보임). 잘 되면 이후엔 --headless
```
Windows 작업 스케줄러 등록(매일 13:00, 실패 시 1시간 뒤 재시도):
```powershell
powershell -ExecutionPolicy Bypass -File scripts\register_task.ps1
```
macOS/Linux 는 `scripts/run_daily.sh` 를 cron 에 등록합니다 (`0 13 * * *`).

수집기는 화면의 메뉴 글자(비즈니스 인사이트 → 판매분석 → 어제 → 엑셀 다운로드 → 상품별 판매 리포트,
광고 관리 → 매출 성장 → 어제)를 따라갑니다. 쿠팡 화면이 바뀌면 **`config/collector.json` 의 글자만 고치면** 됩니다.
실패하면 `data/raw/error_*.png` 스크린샷이 남고, `python -m coupang_calc run --debug` 로 단계별로 멈춰 볼 수 있습니다.

> 주의: 수집기는 실제 쿠팡 화면에서 최종 검증이 필요합니다. 첫 실행은 반드시 창이 보이는 상태로 하세요.
> 로그인 세션이 만료되면 `run` 이 실패하므로 `login` 을 다시 실행합니다.
> 자동 수집이 안 되는 날은 1단계(파일 업로드)로 넣으면 됩니다.

## 구조
```
coupang_calc/
  sales_report.py   상품별 판매 리포트(A~M열) 파서 — 헤더명으로 찾고, 없으면 열 순서로
  ads_report.py     광고센터 캠페인 표 / CSV / 대화식 입력 정규화 (퍼센트·쉼표 처리)
  store.py          SQLite: options, margin_history, sales_daily, ads_daily (날짜 기준 덮어쓰기)
  ledger.py         장부 계산 (캠페인×일자, 월 합계, 마진 이력 적용)
  web.py + static/  Flask 웹 화면
  collector.py      Playwright 자동 수집 (대안)
  mapping.py        config/mapping.csv ↔ DB
extension/          크롬 확장 프로그램 (단독 동작; 선택적으로 /api/records 로 전송)
docs/               초보자 가이드와 화면 이미지
config/mapping.csv  초기 옵션·캠페인·마진 (적용시작일 열로 이력도 표현 가능)
config/collector.json  수집기 메뉴 글자·URL 설정
scripts/            매일 13:00 실행 스크립트 (Windows .bat/.ps1, cron .sh)
tests/              pytest (원본 엑셀 2025-06-08 값과 대조)
```

## 테스트
```bash
pip install -r requirements-dev.txt
python -m pytest -q                 # 파이썬 계산·서버
node tests/js/test_extension.mjs    # 확장 프로그램 계산 로직
```
