# 새 대화창에서 이어하기용 메모

새 Claude Code 세션을 열 때 이 저장소(luvseogenie/-)와 브랜치
`claude/coupang-sourcing-program-2-5tg37d` 를 고른 뒤, 첫 메시지로
"이어하기.md 읽고 이어서 진행해" 라고 적으면 됩니다.

## 프로그램 요약
- 쿠팡 소싱 프로그램 (전업고수/전업초보 프로그램과 비슷하게). Python + FastAPI, 브라우저는 Playwright로 엣지를 붙여서 씀(attach 모드).
- 흐름: 카테고리 수집(120개/페이지, 판매순) → 가격·리뷰 필터 → 윙(prematch API)에서 28일 조회수 → 최근 28일 리뷰 수 × 배수(기본 20)로 28일 판매 추정 → (선택) 상품 상세에서 실제가격·"한 달간 N명 구매"·배송(로켓/로켓그로스/윙) 확인.
- 실행: 1_install.bat → 2_run.bat, 업데이트는 3_update.bat 또는 프로그램 안 업데이트 버튼. 현재 버전은 VERSION 파일.

## 사용자 조건(꼭 지킬 것)
- 코딩 모름. 항상 의논하고 진행. 토큰 아끼기(짧게).
- 웨일은 업무용이라 절대 건드리지 말 것. 프로그램은 엣지 사용.
- 프로그램 브라우저에서 coupang.com 소비자 사이트에 로그인하지 않음. 윙은 안 쓰는 판매자 계정.
- 조건에 안 맞는 상품은 분석하지 않음(시간·토큰 낭비).
- 상세 확인 기반 28일 판매/매출 기능은 없애지 말 것(선택 단계로 유지).

## 현재 막힌 문제 (2026-09-04)
- 프로그램이 띄운 엣지로는 상품 상세 페이지가 Akamai에 차단(Access Denied)됨. 같은 노트북·핫스팟에서 일반 엣지는 열림.
- 지금까지 한 것: 위장 스크립트 제거, 실행 인자 최소화(--remote-debugging-port, --user-data-dir만), rebrowser-playwright, 사람처럼 이동, 딜레이/휴식.
- 남은 차이: 프로그램 전용 새 프로필(쿠키 이력 없음) vs 평소 엣지 프로필. → 다음 시도: 도구 › "내 브라우저 프로필 사용" (엣지 창 전부 닫고) 으로 평소 엣지 프로필을 붙여서 상품 페이지가 열리는지 확인.
- v.54: 로켓직구 오판 수정(해외 판매자여도 goldFish/3PC 면 로켓그로스, 잘못 학습된 뱃지 자동 정리), 리뷰 28일 집계를 쪽 이분 탐색으로 정확하게(56000 같은 과대 추정 해결), 리뷰 추정 중 20개마다 쉬고 403 이면 120초 쉬었다 재개.
- v.56: 상세 확인이 기본 경로. 페이지가 스스로 부르는 가격·판매자 API 응답을 가로채 써서 상품당 요청은 페이지 1번뿐. 차단 시 3→10→20→30분 점점 길게 쉬고 자동 재개. 리뷰 추정은 선택(기본 꺼짐).

## 테스트
- 샌드박스 목 테스트: scratchpad t/test_flow.py, t/test_wing.py (실제 쿠팡 접속은 사용자 PC에서만 가능).
