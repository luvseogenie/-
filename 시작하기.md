# 처음 시작하기 (완전 초보자용)

컴퓨터를 잘 몰라도 따라 할 수 있게 썼습니다. **순서대로만** 하세요.
전부 합쳐서 **20~30분** 걸립니다. (설치는 처음 한 번만)

---

## 준비물 확인

- Windows 또는 Mac 컴퓨터
- Chrome 브라우저
- 인터넷 연결

---

# 1단계. 프로그램 2개 설치하기 (10분)

이 프로그램을 돌리려면 두 가지가 필요합니다. 둘 다 무료입니다.

## 1-1. Python 설치

1. https://www.python.org/downloads/ 접속
2. 노란색 큰 버튼 **"Download Python 3.x.x"** 클릭
3. 내려받은 파일 실행
4. ⚠️ **가장 중요** — 설치 창 **맨 아래**에 있는
   **☑ Add python.exe to PATH** 를 **반드시 체크**하세요
   (이걸 안 하면 나중에 안 됩니다)
5. **Install Now** 클릭 → 끝나면 **Close**

> 맥이면 그냥 다운로드해서 계속 "계속" 누르면 됩니다.

## 1-2. Node.js 설치

1. https://nodejs.org/ 접속
2. 왼쪽에 있는 **LTS** 버튼 클릭 (오른쪽 Current 아니고 **왼쪽**)
3. 내려받은 파일 실행
4. 계속 **Next** 만 누르면 됩니다 (아무것도 바꾸지 마세요)
5. 마지막에 **Install** → **Finish**

## 1-3. 컴퓨터 재시작

설치가 제대로 적용되도록 **컴퓨터를 한 번 껐다 켜주세요.**
(귀찮아도 이걸 하면 문제가 훨씬 줄어듭니다)

---

# 2단계. 프로그램 내려받기 (2분)

1. https://github.com/luvseogenie/- 접속
2. 화면 왼쪽 위에 **브랜치 선택 버튼**(가지 모양 아이콘)을 눌러
   **`claude/coupang-sourcing-mvp-73sxqd`** 선택
3. 오른쪽 초록색 **`< > Code`** 버튼 클릭
4. 맨 아래 **Download ZIP** 클릭
5. 내려받은 ZIP 파일을 **압축 해제**
6. 압축 푼 폴더를 **바탕화면**으로 옮기세요 (찾기 쉽게)

> 폴더 이름이 길면 짧게 바꿔도 됩니다. 예: `쿠팡소싱`

---

# 3단계. 설치하기 (5~10분, 자동)

## Windows

1. 폴더 안의 **`windows`** 폴더를 엽니다
2. 먼저 **`0_진단.bat`** 을 **더블클릭** — 준비가 됐는지 확인해 줍니다
   - `[X]` 표시가 있으면 화면에 적힌 대로 먼저 해결하세요
3. 문제가 없으면 **`1_설치.bat`** 을 **더블클릭**
4. 검은 창에 글자가 주르륵 올라갑니다. **그냥 기다리세요.**
5. `설치가 모두 끝났습니다!` 가 나오면 아무 키나 눌러 닫습니다

> ⚠️ **가장 흔한 실수**: ZIP 파일을 **압축 풀지 않고** 그 안에서 바로 실행하는 것.
> ZIP을 더블클릭하면 폴더처럼 열려서 파일이 보이지만, 거기서 실행하면 실패합니다.
> **ZIP 파일에 우클릭 → "압축 풀기"** 를 먼저 하세요.
>
> `0_진단.bat` 이 이 문제를 자동으로 잡아냅니다.

## Mac

1. 폴더 안의 **`mac`** 폴더를 엽니다
2. **`1_설치.command`** 를 **더블클릭**
3. 기다렸다가 `설치가 끝났습니다!` 가 나오면 닫습니다

### ⚠️ 이런 경고가 뜨면

| 경고 | 해결 |
|---|---|
| **"Windows의 PC 보호"** | **추가 정보** → **실행** 클릭 |
| Mac **"확인되지 않은 개발자"** | 파일 **우클릭** → **열기** → **열기** |
| **"Python이 설치되어 있지 않습니다"** | 1-1로 돌아가서 **Add python.exe to PATH** 체크했는지 확인 후 재설치 → 컴퓨터 재시작 |
| **"ZIP 파일 안에서 실행"** | ZIP에 우클릭 → 압축 풀기 → 풀린 폴더에서 실행 |
| 그 외 설치 실패 | `windows` 폴더에 생긴 **`설치기록.txt`** 를 보내주세요 |

---

# 4단계. 실행하기 (10초)

## Windows

- **`windows`** 폴더의 **`2_실행.bat`** 더블클릭

## Mac

- **`mac`** 폴더의 **`2_실행.command`** 더블클릭

그러면:
- 검은 창 **2개**가 새로 열립니다 → **끄지 마세요!** (이게 프로그램 본체입니다)
- 10초 뒤 크롬에 **대시보드 화면**이 자동으로 열립니다

> 화면이 안 열리면 크롬 주소창에 직접 입력: **localhost:3000**
>
> **프로그램을 끄고 싶을 때**: 검은 창 2개를 닫으면 됩니다.

---

# 5단계. 크롬 확장 등록 (처음 한 번만, 2분)

상품 정보를 읽어오는 부분입니다.

1. 크롬 주소창에 입력: **`chrome://extensions`** → Enter
2. 화면 **오른쪽 위**의 **개발자 모드** 스위치를 **켜기**
3. 왼쪽 위에 새로 생긴 **`압축해제된 확장 프로그램을 로드합니다`** 클릭
4. 아까 압축 푼 폴더에서 → **`extension`** → **`dist`** 폴더 선택 → **폴더 선택**
5. **쿠팡 소싱 수집기** 가 목록에 뜨면 성공
6. 주소창 오른쪽 **퍼즐 조각 아이콘** 클릭 → **쿠팡 소싱 수집기** 옆 **압정** 눌러 고정

---

# 6단계. 실제로 써보기

## ① 상품 목록 모으기

1. 크롬에서 **쿠팡 카테고리 페이지**를 엽니다
   (예: 쿠팡 → 카테고리 → 주방용품 → 조리도구)
2. 상품이 화면에 보이는 걸 확인
3. 주소창 오른쪽 **확장 아이콘** 클릭
4. **`쿠팡 상품 60개 감지`** 같은 문구 확인
5. **[현재 페이지 수집]** 버튼 클릭
6. `60개 중 60개 저장` 이 나오면 성공

여러 카테고리를 돌아다니며 반복하면 계속 쌓입니다.

## ② 판매량 확인하기

쿠팡은 **"한 달간 3,000명 이상 구매했어요"** 문구를 **상품 상세 페이지에만** 보여줍니다.
그래서 후보 상품은 상세 페이지를 열어봐야 합니다.

1. 확장 아이콘 클릭 → **`상품 페이지 열면 자동 수집`** 체크
   (이제 상품 페이지를 **열기만 해도** 자동 저장됩니다)
2. 대시보드(localhost:3000)에서 **[상세 확인 필요]** 버튼 클릭
3. 옆의 **[상위 10개 열기]** 클릭 → 탭 10개가 열립니다
4. 잠깐 기다렸다가 탭들을 닫습니다
5. 대시보드에서 **[새로고침]**
6. 2~5를 반복

## ③ 원하는 상품 찾기

대시보드 왼쪽 **소싱 기준**에서

- **[월 1,000개 이상]** 또는 **[월 500개 이상]** 클릭

오른쪽 표에서 **`조건 통과`** 표시된 상품들이 찾으시는 상품입니다.

---

# 다음부터는

프로그램을 다시 쓸 때는 **4단계만** 하면 됩니다.
(`2_실행.bat` 또는 `2_실행.command` 더블클릭)

설치(3단계)와 확장 등록(5단계)은 처음 한 번만 하면 됩니다.

---

# 잘 안 될 때

| 증상 | 해결 |
|---|---|
| **"아직 설치가 안 되어 있습니다"** | `0_진단.bat` 실행 → 화면에 나온 문제 해결 → `1_설치.bat` 다시 실행 |
| 검은 창이 바로 꺼짐 | `0_진단.bat` 을 실행해 원인 확인 |
| `localhost:3000` 이 안 열림 | 검은 창 2개가 켜져 있는지 확인. 30초 더 기다려보기 |
| 확장에서 `0개 감지` | 쿠팡 페이지를 새로고침(F5) 후 다시 시도 |
| 확장 아이콘이 안 보임 | 주소창 오른쪽 퍼즐 아이콘 → 압정으로 고정 |
| `Python이 없습니다` | 1-1의 **Add python.exe to PATH** 체크 후 재설치 → 컴퓨터 재시작 |
| 그 외 | 검은 창에 나온 **빨간 글씨를 그대로 복사**해서 알려주세요 |

---

# 알아두실 것

- **모든 데이터는 내 컴퓨터에만 저장됩니다.** 외부로 나가지 않습니다.
- **"한 달간 N명 구매"** 는 쿠팡이 직접 표시하는 실제 데이터입니다.
- **"예상 판매량"** (리뷰수 × 20)은 추정치입니다. 실제 판매량이 아닙니다.
- 최근 30일 리뷰수 기반 추정은 **같은 카테고리를 며칠 뒤 다시 수집**해야 계산됩니다.
